{
  "id": "030_dockerfile_generator",
  "desc": "generate dockerfile suggestion",
  "request": {
    "text": "{{REPO_ROOT}} 請呼叫 dockerfile_generator，針對這個 repo（FastAPI/uvicorn）生成適合 Cloud Run 的 Dockerfile，並說明 PORT 設定。"
  },
  "assert": {
    "must_contain": ["dockerfile_generator", "PORT", "uvicorn"],
    "must_not_contain": ["Traceback"]
  }
}
