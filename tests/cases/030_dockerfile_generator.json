{
  "id": "030_dockerfile_generator",
  "request": {
    "text": "For repo {{REPO_ROOT}}, generate a Cloud Run compatible Dockerfile. Use tools as needed. Include PORT handling."
  },
  "assert": {
    "status": 200,
    "tool_must_include": ["dockerfile_generator"],
    "must_contain": ["PORT", "uvicorn", "Dockerfile"],
    "must_not_contain": ["Traceback", "Internal Server Error"]
  }
}
