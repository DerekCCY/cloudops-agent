{
  "id": "050_cloudrun_config_generator",
  "request": {
    "text": "For repo {{REPO_ROOT}}, generate Cloud Run templates (service.yaml and cloudrun_deploy.sh). Do NOT deploy. Use tools as needed."
  },
  "assert": {
    "status": 200,
    "tool_must_include": ["cloudrun_config_generator_tool"],
    "must_contain": ["service.yaml", "cloudrun_deploy.sh"],
    "must_not_contain": ["Traceback", "Internal Server Error", "I deployed"]
  }
}
